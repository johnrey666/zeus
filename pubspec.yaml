name: zeus
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # ✅ Firebase core and auth (latest stable compatible versions)
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.3
  cloud_firestore: ^4.17.5
  image_picker: ^1.0.4
  fl_chart: ^0.63.0
  table_calendar: ^3.0.10
  google_fonts: ^6.2.0
  intl: 0.20.2
  csv: ^5.0.2
  path_provider: ^2.1.2
  font_awesome_flutter: ^10.8.0
  iconly: ^1.0.1
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
